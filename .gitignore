node_modules
dist
dist-ssr
*.local

# Vite / Vitest dep caches (kept separate via the cacheDir trick)
node_modules/.vite
node_modules/.vite-temp
node_modules/.vitest-cache

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local secrets (never commit tokens or client secrets)
.env
.env.local
